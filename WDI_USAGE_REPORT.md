# WDI usage report

Classification: `PILOT_ONLY` · `NON_RELEASE` · `NON_EVIDENCE_AUTHORITY`  
Host: Cursor  
Project: Ihor Havryleiko trainer prototype

This report separates what WDI actually contributed from host decisions. It does **not** close human gates.

## WDI-derived decisions

- Followed `SHARED_EXECUTION_CONTRACT_V1`: classify → knowledge map pointer → artifacts → implement → render → critic → remediate (≤3).
- Wrote intake, capability resolution, reference DNA, design contract, pre-implementation review, implementation contract, asset inventory, checkpoints, critic, remediation, observability under `wdi/`.
- Reference DNA classes: preserve cinematic scale / oversized type / premium dark field; adapt club orange to water azure; do not copy orange interstitial, FIT&YOU, or membership IA.
- Design contract: one signature motion (intro), editorial current layout, no fake testimonials, no WCAG/release claims.
- Tool lane for this work: `EXPLORATION_OR_IMPLEMENTATION`. Screenshots are evidence of render; interpretations are model findings.
- Phase 10B package routing left as `DELEGATED_TO_PHASE_10B_ROUTER`. No production skill packages were executed as authority.

## Host implementation decisions

- Stack: Vite + React + TypeScript + `motion/react` (not `framer-motion`). Not a WDI framework lock.
- Output only under `Proto/Cursor_VDI`.
- Ukrainian UI from the dataset; Latin brand spelling matches the supplied logo (`Igor Gavrileyko`).
- Results-board pricing instead of membership cards.
- Client-side form success state only.
- Git init inside `Cursor_VDI` because the trainer project had no own repo.

## Host visual / art-direction decisions

- Palette: abyss / navy / azure / aqua / foam. Gold only on the intro mark.
- Type: Unbounded + Manrope (Cyrillic). ui-ux-pro-max suggested Barlow + portfolio grid; **rejected** as generic sports default.
- Signature: paddle-strike 2D silhouette through the IG mark, then clip-path split into ocean video. Not a 3D Poseidon (too gimmicky for this prototype).
- Story as three vertical films (canoe / champion poster / gym), not a numbered SaaS card row — numbering used only because the path is a real sequence.
- Proof = titles + certificates + coaching footage. No invented client bodies.

## Manual fallback decisions

- Railway: `RAILWAY_BLOCKED_NO_AUTH`.
- Push: `PUSH_BLOCKED_NO_REMOTE`.
- Gold logo white rectangle: Python/PIL knockout to PNG.
- MOV → muted 720p MP4 via ffmpeg (`self-train.mp4`, `presentation.mp4`).
- Dragonboat story cell: video first frame was unreliable/black; replaced with real `Champion.jpg`.
- Autoplay videos: poster as CSS background; video fades in on `playing`; pause when offscreen.
- IDE browser MCP became unavailable mid-pass 2; Playwright CLI captured `screenshots/`.
- Motion+ gated source not used. Free examples used: [exit animation](https://examples.motion.dev/react/exit-animation), [AnimatePresence modes](https://examples.motion.dev/react/animate-presence-modes).

## What was not claimed

- Not WCAG conformance.
- Not legal/medical advice.
- Not production release approval.
- Not evidence-grade Phase 10B validation (no axe/Lighthouse/OSV run as a gate).
- Context7 was not used (F8 / unbound evidence surface for WDI evidence paths).
